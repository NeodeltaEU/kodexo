{
  "$schema": "http://json-schema.org/schema",
  "$id": "SchematicsUminilyFeature",
  "title": "Feature Schema",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the feature.",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name would you like to use for the feature?"
    }
  },
  "required": ["name"]
}
